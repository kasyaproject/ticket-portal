{
    "version": 2,
    "build": [
        {
            "src": "src/index.ts",
            "use": "@vercel/node",
            "config": {"includeFile": ["src/**"]}
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/index.ts"
        }
    ]
}